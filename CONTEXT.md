# 领域上下文

## 目标与边界

- **目标公司**：系统第一阶段只服务一个目标公司。
- **主体**：目标公司下的国内分支机构或分支主体；具体主体清单和城市信息待补充。
- **工资数据管理系统**：第一阶段维护员工月薪，以考勤表和绩效表为月度输入，生成工资记录，按主体和期间保存、查询、汇总及导出四类工资 Excel。
- **个税计算**：明确不在当前范围内；个税字段只允许来自外部数据或按规则留空。

## 技术实现决策（2026-09-12）

- **运行形态**：当前先以本地网页端运行，浏览器访问本机 Web 服务，默认使用 `127.0.0.1`；暂不做安装包、PyInstaller 或单个 EXE。
- **后端**：Python 3.13、FastAPI、Uvicorn、Pydantic v2。
- **数据库**：PostgreSQL；使用 SQLAlchemy 2.x、Alembic 和 psycopg 3；数据库运行在本机，数据不上传云端。
- **前端**：React、TypeScript、Vite、Ant Design；数据请求使用 TanStack Query，表单使用 React Hook Form。
- **Excel**：使用 `openpyxl` 处理 `.xlsx`；旧式 `.xls` 税务模板转换为 `.xlsx` 后作为运行时模板。
- **测试**：pytest、Vitest、Testing Library、Playwright。
- **数据约定**：业务计算使用 `Decimal`，数据库金额使用 `NUMERIC(18, 2)`；身份证号码和银行卡号等业务标识按文本保存。
- **后续部署**：如改为远程部署或多人访问，需要重新评估数据边界、账号认证、权限和备份方案。

## 核心术语

- **员工**：人员主档。员工以身份证号码作为业务唯一标识；当前只保留最新资料。
- **任职关系**：员工与主体、部门之间的当前关联，承载主体、部门、职位及生效信息；月中变化按生效日期处理，历史工资记录保存快照，不建立员工资料历史版本。
- **主体**：公司内部的分支机构或分支主体，是工资数据的主要归属和查询维度；第一版暂不区分法律类型。
- **部门**：公司共用的部门体系；同名部门可以分别存在于主体 A 和主体 B，并支持上下级关系。
- **薪酬结构**：实际月薪由固定薪酬和绩效部分组成；绩效表直接提供系数，实际绩效金额以绩效部分基数乘以系数得到。
- **工资项目**：工资明细中的收入、扣款和汇总项目；固定薪酬、绩效、考勤扣款、社保、公积金和考勤激励是主要项目。
- **考勤规则**：应出勤天数从考勤表读取；迟到/早退按分钟、请假按天数，P7 及以上不参与任何考勤罚款；忘打卡每次扣 30 元，补卡后免扣。
- **考勤激励**：2026 年 7 月将所有主体 2026 年 6 月锁定考勤扣款平均分给符合条件的 7 月正式转正员工；参与者须职级低于 P7，分钱不区分职级。
- **导入批次**：一次 Excel 导入的完整记录，保留原始文件、模板版本、字段映射、行级状态和错误信息。
- **工资记录**：由输入数据和规则生成的规范化工资事实；确认后原记录不可直接修改，错误通过更正批次处理。
- **工资期间**：按自然月记录工资所属期间；正常工资和补发使用不同发放批次。
- **基数**：社保、公积金均以固定薪酬为缴费基数，按员工 base 地匹配城市规则。

## 已确认的业务决策

- 工资明细由 Excel 提供，系统不负责个税计算。
- 工资计算的核心输入是员工维护的最新固定薪酬和绩效部分基数、每月考勤表和员工绩效表；绩效表提供系数，系统据此生成实际月薪，再导出四个 docs 模板。
- 员工主档只需维护最新固定薪酬和绩效部分基数；月薪结果按工资期间保存，不要求员工资料历史版本。
- 四个 docs Excel 模板全部属于 MVP 输出范围；系统保持模板结构、格式、工作表和已有正确公式。税额不由系统计算，无法得到的字段留空并在导出报告提示。
- 考勤扣款根据员工固定薪酬金额折算；应出勤天数从考勤表读取，节假日、调休和加班由考勤表先处理；每日标准工时按 8 小时，迟到/早退按分钟且使用同一公式，请假按天数计算；同日多个异常由考勤表先合并；有薪/无薪只用于请假：有薪假不扣固定薪酬，无薪假按天数折算；忘打卡每次扣 30 元，补卡后免扣；考勤表提供事实，系统统一重算扣款；P7 及以上员工对所有考勤处罚免扣；所有考勤扣款必须在工资记录中标注异常类型和来源。
- 2026 年 7 月考勤激励是按月复用规则的首次实例：将上月所有主体合计的考勤扣款，平均分给本月有考勤记录、无迟到/早退/忘打卡/请假、正式、已转正且职级低于 P7 的员工；分钱不区分职级，按符合条件人数平均，保留 2 位小数，尾差给排序最后一名；激励计入应发和实发，不计入社保公积金基数。P7 及以上员工不参与考勤罚款，也不参与考勤激励分钱。
- 输入至少包括员工基础资料/固定薪酬/绩效基数、考勤记录和绩效记录；每个员工每个主体每个工资期间有一个正常工资发放批次，补发或其他发放使用独立批次。
- 工资金额规则：工资项目和最终结果保留 2 位小数并四舍五入，中间计算保留高精度。
- 绩效表直接提供最终绩效系数；系数必须为非负数，不设固定上限；缺失系数直接报错，该员工不生成正式工资结果。
- 城市规则缺失或已过期时阻止工资计算和导出，先补充有效规则。
- 同一主体、工资期间、员工和批次类型重复导入时直接拒绝；需要明确发起更正才生成新批次。
- 社保和公积金第一版只使用城市规则，不允许主体或员工覆盖缴费比例或固定金额。
- 社保和公积金按城市规则计算，均以员工固定薪酬为缴费基数；绩效、考勤扣款和考勤激励不计入基数；所有险种和公积金统一使用固定薪酬来源，但各险种可按城市规则配置比例和上下限；结果只保存系统计算值，不设置人工结果对照字段。城市规则需要拆分社保各险种、公积金公司/个人比例及基数上下限。
- MVP 需要覆盖基础资料、Excel 导入、工资计算、工资台账、查询汇总、四类目标输出、对照核对、社保公积金基数管理和历史追溯；具体模板字段以 docs 中的四个文件为准。
- 第一版采用固定 Excel 模板，并提供模板下载。
- 导入批次允许正确行先导入，错误行单独列出；错误行保留在批次中，修正并重新校验通过后才可进入正式台账。
- 确认后的工资记录不可直接修改；需要通过更正批次处理。重复导入默认作为新批次保存并提示，由用户选择新增或更正。
- 当前使用者只有一个人，暂不需要复杂登录和权限。
- 原始 Excel、模板版本、字段映射、行级导入状态和错误信息、规范化数据、更正记录全部保留。
- 员工的业务唯一标识是身份证号码，身份证号码不可修改；当前只维护员工主表，不建立员工资料历史版本。工资记录必须保存计算时使用的主体、部门、职位、固定月薪、银行卡、基数等快照。
- 主体先作为工资归属单位使用，不区分法律类型；主体准确清单和城市信息后续提供。
- 月中调薪、入离职、主体调动和部门调动按生效日期分段，固定薪酬按自然日折算，主体/部门按实际生效日期归属；历史工资结果保存计算快照。
- 工资批次状态采用草稿→试算→确认→锁定→导出；锁定后只能通过更正批次生成新版本，期间汇总取最新有效版本。
- 部门体系在主体之间共用，但同名部门可分别存在于不同主体；支持部门上下级关系。
- 查询支持工资期间、主体、部门、员工和导入批次；默认按主体和工资期间汇总。
- 社保、公积金由城市社保基数表提供规则，员工名单中的 base 地决定适用城市；实际基数和城市规则的具体字段、上下限及生效日期待确认。
- 四个 docs Excel 模板是当前 MVP 的目标输出候选；税务模板的具体处理方式待确认。

## 模板观察结果

- `人工成本表-模版.xlsx`：包含按主体分组的人工成本汇总、工资/个税/社保等汇总，以及项目投入工时和人工成本明细区域。
- `代发工资模板.xlsx`：包含多个主体工作表，常见字段为身份证号、序号、员工卡号/账户、金额、备注；不同主体工作表存在格式差异。
- `工资表模板.xlsx`：按主体和期间输出实发工资、个税、社保公司/个人、公积金公司/个人、应发工资等字段，并带月度项目区域。
- `正常工资薪金所得_模板.xls`：原始旧式税务模板，仅作为转换和结构核对的参考；运行时统一使用转换后的 `.xlsx` 模板。模板中存在公式、外部引用和 `#N/A`/`#REF!` 示例结果，不能直接当作纯数据表处理。

## 待确认

- 工资日折算时，月中分段的日数分母（自然日、应出勤天数或其他约定）。
- 7 月考勤激励是否长期复用，以及主体变更时的归属。
- 城市规则缺失或过期时是否阻止计算。
- 实际发放日期是否必填。
- 草稿、试算、确认、锁定、导出状态的具体流转和锁定后更正的汇总取值。
- 考勤表应出勤天数异常值和输入校验失败的处理级别。

