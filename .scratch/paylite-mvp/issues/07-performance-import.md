# 07：绩效导入与错误修正

**What to build（交付）：** HR 能导入期间绩效系数，区分缺失、错误和有效零值，并修正错误行。

**Blocked by（前置任务）：** 02：员工与薪酬维护；05：工资期间与批次工作台

**Status:** completed

## 验收标准

- [x] 提供固定模板下载、上传摘要、行级错误及修正重验。
- [x] 系数必须非负且不设固定上限；缺失值与有效零值明确区分。
- [x] 保留原文件、模板版本、原始行与规范化系数，重复事实不静默覆盖。
- [x] 正确行先保存、错误行待修，变更可被试算失效机制识别。
- [x] 验证缺失、零值、负数、大于一的系数、重复导入及修正重试。

## 开始前需收敛的事项

试用期不考核绩效、不发绩效奖金、无需绩效记录已确认；转正当月按整月绩效规则处理。复用已有导入能力但不人为增加对任务 04 或 06 的依赖。

## 完成记录

- 实现摘要：新增 `TPL-PERF-v1` 固定模板、绩效导入与逐行纠错 API，保留原始文件、原值、规范化系数及更正历史；新增 `0005` 迁移和可审查 SQL，把绩效系数调整为无限定精度的 `NUMERIC`，有效事实入库时递增 `performance_input_revision`。导入页复用月度导入布局，提供期间和主体批次选择、预览、历史与纠错。试用期员工不要求绩效记录，若误导入则按行报错；转正当月需系数。
- 验证命令与结果：专用 `paylite_test` 执行 `backend/.venv/bin/pytest -q`（37 passed，含缺失、零值、负数、大系数、重复文件/事实、公式、逐行修正与重试）；改动范围 Ruff 检查与格式检查、`lint-imports`、`compileall` 通过。Node 24 下 `tsc -b`、`vitest run`（21 passed）、`vite build` 通过。Playwright 真实浏览器核对绩效页空状态、接口错误、禁用上传及 390px 窄屏无横向溢出；成功与纠错状态由前端测试覆盖。
- 审查结论及遗留事项：重复绩效事实不覆盖。08 号试算尚未实现，届时须使用 `performance_input_revision` 使旧试算失效，并沿用试用期无需绩效记录、无绩效奖金的口径。浏览器核对时后端 API 未运行，因此未做真实浏览器上传；上传与纠错已在 PostgreSQL 集成测试中通过。
