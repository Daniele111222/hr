# 05：工资期间与批次工作台

**What to build（交付）：** HR 能创建自然月工资期间和主体正常工资批次，查看批次状态与应处理员工范围。

**Blocked by（前置任务）：** 01：公司与组织维护

**Status:** completed

## 验收标准

- [x] 期间年月与自然月起止日一致，支持查询已有期间和批次。
- [x] 同主体同期间重复创建正常批次被拒绝，修改批次号不能绕过约束。
- [x] 页面展示批次状态、员工范围及数据准备情况；已存在员工时能够关联，不要求先完成员工 Excel 导入。
- [x] 后续算薪所用全体员工范围有明确来源，不能只以导入成功行作为完整名单。
- [x] 并发或重复创建不会产生两个正常批次；创建失败整体回滚。
- [x] 实际发放日期的必填时点未确认前不擅自设置正式导出门槛。

## 开始前需收敛的事项

收敛员工范围的取得与刷新契约，并保证接入任务 02 后可按任职与期间验证；任务 05 可先独立验证期间与批次流程。

## 完成记录

- 实现摘要：已实现工资期间自然月创建/查询、主体正常工资批次创建与重复保护、期间有效任职员工范围、考勤/绩效/城市规则准备情况及 `/payroll` 工作台页面。修复首个批次无法选择主体、05 提前开放补发批次、空员工范围误报缺失准备数据三个问题。
- 验证命令与结果：专用 `paylite_test` 上 `backend/.venv/bin/python -m pytest -q`：30 passed；`backend/.venv/bin/ruff check src tests`、编译检查、`lint-imports` 均通过；前端全量 Vitest：16 passed；前端 `pnpm build` 通过。
- 审查结论及遗留事项：已完成工作区代码审查并修复相关功能问题，并将期间/批次查询与写入事务收回 service。OpenAPI 类型生成、URL 筛选状态和原型视觉一致性属于后续整改项，不阻塞本票已定义的功能验收。
