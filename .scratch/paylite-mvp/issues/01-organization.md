# 01：公司与组织维护

**What to build（交付）：** HR 能在本机网页初始化目标公司，维护城市、工资归属主体、共用部门及主体部门实例。

**Blocked by（前置任务）：** 无（可立即开始）

**Status:** completed

## 验收标准

- [ ] 前端基础工程可启动并访问本机后端，支持加载、空数据、提交成功和错误反馈。
- [ ] 公司按单一目标公司管理，主体、城市、部门及主体部门关系能够新增、查看和修改。
- [ ] 重复编码、部门自引用或循环、跨公司关系、被引用数据删除被拒绝并显示明确原因。
- [ ] 一次业务写入整体成功或回滚；金额及业务标识的接口约定沿用已确认设计。
- [ ] 页面操作、HTTP 错误映射与数据库约束均有针对性验证。

## 开始前需收敛的事项

无额外业务决策。随首条真实用例接入会话依赖、统一错误映射和事务编排，不预建空业务模块。

## 完成记录

- 实现摘要：已接入单目标公司、城市、主体、部门、主体部门关系的查询/新增/修改 API；校验同公司关系、部门自引用/循环（数据库触发器）、唯一编码和统一 409 错误映射；增加被引用数据删除保护；组织页面支持各类资料新增和编辑。
- 验证命令与结果：后端 `ruff check src tests` 通过；前端 `tsc -b`、Vitest（1 个测试文件/1 个测试）和 Vite production build 通过；使用隔离 PostgreSQL `paylite_test` 运行后端 pytest：22 passed（含组织/员工 API 流程和数据库约束集成测试）。
- 审查结论及遗留事项：组织与员工基础维护竖切已完成；工资计算、导入和后续期间流程不属于本需求范围。

## Comments

2026-09-20（索引同步时记录，非实现方）：`Status:` 保持 `completed`，但 development-plan.md 状态列记为**待验收**，理由是验收标准 5 条均未勾选，且第 5 条“页面操作、HTTP 错误映射与数据库约束均有针对性验证”缺前端证据——实测 `frontend/src` 下仅有 `src/app/App.test.tsx`（1 个测试文件 / 1 个测试），`pages/organization/` 无测试文件，与完成记录自述的 Vitest 结果一致。第 1—4 条的后端部分（唯一编码、部门自引用/循环触发器、跨公司关系、被引用数据删除保护、事务回滚）有 `test_postgres_integration.py` 等集成测试支撑，但同样未逐条勾选并标注对应测试。

待办：补组织页面组件测试（加载/空数据/提交成功/错误反馈），逐条勾选验收标准并标注证据，随后把 development-plan.md 状态列转“已完成”。勾选须由实际运行过验证的会话执行。

2026-09-21：补充 `pages/organization/index.test.tsx`，覆盖加载、空数据初始化、提交成功刷新和后端业务错误反馈；与员工页及应用测试一起实测为 3 个测试文件、8 个测试通过，`oxlint`、`tsc -b` 和 Vite production build 通过。Uvicorn 健康检查已返回 200 `{"status":"ok"}`，但按用户要求未继续执行 Vite 到后端的代理连通性检查，因此第 1 条仍不勾选。PostgreSQL 未下载，涉及组织 CRUD 持久化、数据库约束和事务回滚的集成测试本次仍跳过，第 2—5 条也保持未勾选，索引状态仍为“待验收”。
